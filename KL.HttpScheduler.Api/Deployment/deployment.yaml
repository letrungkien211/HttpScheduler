apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: #{NAMESPACE}#
  name: #{API.IMAGENAME}#
spec:
  replicas: #{Replicas}#
  selector:
    matchLabels:
      app: #{API.IMAGENAME}#
  template:
    metadata:
      labels:
        app: #{API.IMAGENAME}#
    spec:
      containers:
      - name: #{API.IMAGENAME}#
        image: #{ACRNAME}#.azurecr.io/#{API.IMAGENAME}#:#{Build.SourceBranchName}#-#{Build.BuildId}#
        env:
          - name: ASPNETCORE_ENVIRONMENT
            value: '#{ASPNETCORE_ENVIRONMENT}#'
		  - name: ApplicationInsights:InstrumentationKey
			value: '#{InstrumentationKey}#'
		  - name: Config.RedisConnectionString
		    value: '#{RedisConnectionString}#'
        ports:
        - containerPort: 80
      imagePullSecrets:
        - name: #{ACRNAME}#