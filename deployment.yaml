apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: #{NAMESPACE}#
  name: #{IMAGENAME}#-api
spec:
  replicas: #{Replicas}#
  selector:
    matchLabels:
      app: #{IMAGENAME}#-api
  template:
    metadata:
      labels:
        app: #{IMAGENAME}#-api
    spec:
      containers:
      - name: #{IMAGENAME}#-api
        image: #{ACRNAME}#.azurecr.io/#{IMAGENAME}#:#{Build.SourceBranchName}#-#{Build.BuildId}#
        env:
          - name: ASPNETCORE_ENVIRONMENT
            value: '#{ASPNETCORE_ENVIRONMENT}#'
		  - name: ApplicationInsights:InstrumentationKey
			value: '#{InstrumentationKey}#'
		  - name: Config.RedisConnectionString
		    value: '#{RedisConnectionString}#'
        ports:
        - containerPort: 80
      imagePullSecrets:
        - name: #{ACRNAME}#